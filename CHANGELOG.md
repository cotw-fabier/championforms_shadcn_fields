## 0.0.2

* Text-family fields (text input, text area, autocomplete, number input,
  chip input) now fall back to the core `requestFocus` flag when `autofocus`
  is not set, matching championforms 0.7.1's behavior. An explicit
  `autofocus:` still wins.

## 0.0.1

* TODO: Describe initial release.
